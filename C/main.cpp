#include <stdio.h> // standard input output. header 라이브러리 추가
int main(){ // 프로그램 실행 로직의 핵심 함, 순서대로 코드 읽음
    printf("Hello World!"); // 라이브러리 함수 호출
    return 0; // 함수 종료시 반환값 선언, main 함수 반환 데이터타입이 int
}
